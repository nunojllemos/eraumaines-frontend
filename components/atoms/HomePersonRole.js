const HomePersonRole = ({ role }) => {
    return <span className='text-16 text-[var(--text-color)] uppercase font-light font-montserrat-alternates opacity-40 block mt-4'>{role}</span>
}
export default HomePersonRole
